{
  "name": "finance-system",
  "version": "1.0.0",
  "description": "Personal Finance and Expense Tracking Web System",
  "scripts": {
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
